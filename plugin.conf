# Spamtroll DirectAdmin Plugin Configuration
#
# This file defines the plugin metadata for DirectAdmin.
# See: https://docs.directadmin.com/developer/plugins/

# Plugin identification
name=Spamtroll Anti-Spam
id=spamtroll
version=1.1.1

# Author information
author=Spamtroll.io
author_email=support@spamtroll.io
author_url=https://spamtroll.io

# Plugin description
description=AI-powered spam detection for incoming emails. Uses machine learning (RETVec + GPT) to detect and block spam with high accuracy.

# Update URL for automatic updates.
# DA periodically fetches version_url; if the returned version is newer
# than `version=` above, the Plugin Manager shows "Update available"
# and can download + install update_url with one click.
#
# The spamtroll frontend nginx image (web/Dockerfile.prod) clones this
# repo at build time and serves both files under /download/.
update_url=https://spamtroll.io/download/spamtroll-directadmin.tar.gz
version_url=https://spamtroll.io/download/spamtroll-directadmin.version

# Plugin requirements
min_directadmin_version=1.60.0

# Plugin state
active=yes

# Pluggable menu (Evolution skin)
menu_admin=images/menu.json

# Access levels
# admin = shown to admins only
# reseller = shown to admins and resellers
# user = shown to all users
access=admin
